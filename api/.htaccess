# Allow API access
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow PHP execution
<Files "chat.php">
    Require all granted
</Files>

# Ensure PHP is processed
<IfModule mod_php.c>
    php_flag display_errors Off
</IfModule>
